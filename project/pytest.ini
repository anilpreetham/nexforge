[pytest]
DJANGO_SETTINGS_MODULE = config.settings.dev
python_files = tests.py test_*.py *_tests.py
# temp-nexforge-auth is a separate scratch project; keep it out of collection.
norecursedirs = temp-nexforge-auth .venv .git
